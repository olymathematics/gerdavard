فرض کنید $d(n)$ تعداد مقسوم‌علیه‌های مثبت عدد صحیح مثبت $n$ را نشان دهد. اگر $k$ یک عدد صحیح مثبت فرد داده‌شده باشد، ثابت کنید که یک تصاعد حسابی صعودی از اعداد صحیح مثبت $(a_1, a_2, \dots, a_{2019})$ وجود دارد به طوری که
\[
\gcd\bigl(k,\, d(a_1)d(a_2)\cdots d(a_{2019})\bigr) = 1.
\]
